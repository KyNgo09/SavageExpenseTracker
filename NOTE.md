**Những việc phát triển sau:**
- `Lấy thẳng ID từ JWT token cho Expense, Challenge, User, để tránh tình trạng User A gọi API xóa dữ liệu của User B`
- `PUT /api/users/{id} || me`
- `Người dùng không cần thoát ra vào lại mà bảng xếp hạng tự nhảy số khi có ai đó vừa thêm Expense mới`